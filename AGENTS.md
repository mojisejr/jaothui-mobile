# JAOTHUI Mobile Agent Notes

This repo is a standalone Site repo under `/Users/non/dev/opilot/projects/jaothui-mobile`.

Rules:
- Keep Oracle memory in `/Users/non/dev/opilot/ψ/`, not in this repo.
- Consume only curated `/api/mobile/v1/*` endpoints from `jaothui-frontend`.
- Never commit secrets. `EXPO_PUBLIC_*` values are client-visible.
- Work on feature branches and open pull requests into protected `main`.
- Phase 3 can close `rn-static-eye`; Expo Go and native proof require separate runtime evidence.
